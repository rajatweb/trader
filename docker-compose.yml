services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: npm run start
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.next.rule=Host(`trader.rajatweb.com`)"
      - "traefik.http.routers.next.entrypoints=websecure"
      - "traefik.http.routers.next.tls=true"
      - "traefik.http.services.next.loadbalancer.server.port=9000"